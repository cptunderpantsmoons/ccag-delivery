# Helm Packaging Stub

This folder is the reserved location for the CCAG Delivery Helm chart.

Planned contents:

1. `Chart.yaml`
2. `values.yaml`
3. `templates/*.yaml` for deployments, services, PVCs, config maps, and secrets references

Until chart templates are added, use the compose/systemd path from `deploy/ONPREM_RUNBOOK.md`.

